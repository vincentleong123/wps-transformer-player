{
  "name": "wps-transformer-player",
  "version": "1.0.0",
  "description": "WPS-Transformer Player — standalone Express player that masks external tube sources, ingests JSON/TXT URL lists, and plays with YouTube-style scrubbing previews.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "ingest": "node scripts/ingest.js",
    "sprites": "node scripts/generate-sprites.js",
    "check": "node --check server.js && node --check lib/transformer.js && node --check lib/thumb.js && node --check lib/preview.js && node --check lib/stats.js && node --check lib/ingest.js && node --check lib/sprites.js && node --check lib/vast.js && node --check public/js/player.js"  },
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  }
}
